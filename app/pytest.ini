[pytest]
testpaths = tests
pythonpath = src
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
addopts = -q --strict-markers
markers =
    integration: needs a live PostgreSQL (skipped automatically when absent)
filterwarnings =
    ignore::DeprecationWarning
